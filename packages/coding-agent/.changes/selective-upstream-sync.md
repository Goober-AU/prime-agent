- Changed harness memories to in-context digests so refinements preserve the stable system prompt while native compaction keeps current memories and opaque checkpoints intact.
- Added model-visible notices for refinements that were actually applied, without replacing durable refinement outcomes or weakening strict repair validation.
- Fixed active goals failing to resume after successful manual compaction while preserving cancellation, queued input and child-recovery behavior.
- Fixed root requests waiting on a cancelled turn's child-event boundary.
- Added read compatibility for older provider checkpoints and child-continuation records, with explicit errors for uncorrelatable recovery instead of rewriting saved state or replaying work.
- Fixed saved sessions being hidden by legacy inactive-view defaults while retaining explicit Alt+I collapse and stable catalog loading.
- Fixed assistant Markdown links to resolve against the attached session directory, including Windows paths.
- Fixed prompt argument substitution reinterpreting literal dollar sequences inside argument values.
- Fixed LiteLLM maximum-context rejection recovery to compact the conversation instead of retrying the unchanged oversized request.
- Changed the default Prime Inference model to GLM 5.3 without replacing saved model selections.
