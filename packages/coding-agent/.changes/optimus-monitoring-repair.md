- Fixed delayed parent and sibling messages by delivering compatible queued agent messages together at safe turn boundaries, preserving durable receipts and human input priority.
- Made restored-draft feedback transient and draft-scoped without clearing unrelated warnings or losing drafts.
- Avoided redundant recursive session-tree copies and added long-chain regression coverage, incorporating PR #48.
- Added privacy-safe client render-pass, decoded-input, prompt acknowledgment and menu-open timing measurements.
- Made gateway admission FIFO and wake on slot release, with explicit wait-reason diagnostics and unchanged quota safeguards.
- Reduced Python snapshot pauses for custom objects and functions capturing large builtin data graphs while preserving aliases, mutations and recovery checks.
- Added privacy-safe snapshot timing totals for retained and skipped variables so expensive saves can be diagnosed without logging variable names or contents.
- Added bounded recovery for summaries that exhaust their output allowance without accepting partial handoffs or replaying chat and tool work.
- Cleared obsolete compaction failure notices after successful compaction without hiding unrelated errors.
- Fixed failed Bash pipeline producers appearing successful on Windows and retained genuine exit failures on all platforms; intentional output truncation now requires explicit handling of the expected broken-pipe status.
