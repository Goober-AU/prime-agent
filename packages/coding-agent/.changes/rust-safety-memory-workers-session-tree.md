- Fixed overlapping Rust memory imports during long requests and safe recovery after a crashed lock holder.
- Fixed oversized Unicode memory-import chunks without splitting or losing characters.
- Fixed forced Windows worker shutdown after a failed stop request while protecting replacement processes and supervisor ownership.
- Fixed session-tree decoding and lost parent links, with explicit errors for malformed responses instead of an empty tree.
- Fixed cross-worker peer discovery and message delivery, preserving rejection and queue status without replaying uncertain sends.
- Fixed crashed-worker recovery to preserve interrupted work and uncertain cleanup records without blocking unrelated sessions.
- Fixed cancelled owner schedules and stale input pauses surviving worker shutdown or recovery.
- Added disk-backed, backpressured session attaches for clients that support chunked snapshots while preserving legacy clients.
- Fixed heartbeat change notifications and fresh cached results during transient worker disconnects.
- Fixed session renaming freezing workers when subscribers read the saved name.
- Fixed refinement cancellation reaching in-flight provider planning and review without applying cancelled results.
- Added detailed Python kernel and snapshot measurements to the existing performance monitor.
- Fixed a multi-threaded compaction race that could resume the model before compaction starts.
- Fixed completed chunked chat transfers being replaced by their empty metadata header.
