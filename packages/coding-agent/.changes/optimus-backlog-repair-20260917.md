- Fixed busy waiting while queued input is blocked by compaction, retry, or shell work.
- Prioritized queued human input over background messages while preserving human-message and recovered queue order.
- Improved text compaction with bounded output beginnings and endings and structured Python file-edit records.
- Improved `/btw` cache compatibility and model-aware reasoning while preventing tool execution.
- Added bounded `rlm.collect` child-result previews without additional messages in the parent queue.
- Replaced repeated Windows process-identity shell queries with native lookups and hid the remaining daemon process helper console.
- Preserved retained conversation storage during token updates while refreshing authoritative model and effort metadata.
- Kept hidden model context out of live and reopened chat transcripts.
- Distinguished background helpers from active agent work without weakening session lifecycle protections.
- Enabled extension dialogs, including `/telegram`, in chats opened from the session browser.
- Kept session navigation responsive during opening and reconnecting, with cancellation that leaves the underlying agent running.
- Returned to the session browser without waiting for viewer detachment and bounded event processing so streaming cannot starve keyboard input or rendering.
- Removed opposing supervisor lock acquisition during worker recovery and client disconnection.
- Shared concurrent daemon reconnect attempts and cleaned up cancelled attachment transports without aborting the session.
- Kept streamed snapshot contents and event cursors consistent, rejected superseded transfers, and replayed bounded attachment updates in order without per-token transcript downloads.
- Made attachment-buffer overflow fail visibly and invalidated cached state when metadata or history identities require an authoritative refresh.
- Disabled automatic replacement of reachable stale daemons: explicitly shut down the existing daemon before replacement; manual shutdown and force-shutdown remain available.
- Completed and serialized metrics file drains before reporting flush success, preserving reliable `/monitor` recording and immediate reads.
