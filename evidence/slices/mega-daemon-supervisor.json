{
 "slice": "mega-daemon-supervisor",
 "crate": "pi-coding-agent",
 "files": [
  {
   "src": "packages/coding-agent/src/modes/daemon/daemon-supervisor.ts",
   "rust": "crates/pi-coding-agent/src/modes/daemon/daemon_supervisor.rs",
   "loc": 7389
  }
 ],
 "loc": 7389,
 "note": "RE-SLICE (post-crash audit). daemon-supervisor.ts is 7389 LOC and the previous attempt produced only 11KB with 2 of 29 functions and NO DaemonSupervisor class - it is a stub in disguise, the biggest gap in the port. The file currently on disk is a WRONG placeholder: delete it first, then port properly in SEVEN sequential passes.\nNEVER read the whole file. For each pass, read only that line range of packages/coding-agent/src/modes/daemon/daemon-supervisor.ts, then APPEND the Rust for it:\n  1: lines    1-1100  - module header, imports, the 5 error classes (SupervisorRecoveryCancelledError, SnapshotLoadInvalidatedError, WorkerStopTimeoutError, WorkerDescriptorTransitionStaleError, PreRosterWorkerError) and the small helpers (handshakeBudgetMs, delay, unrefDelay, isSupervisorGenerationStale, isSupervisorRecoveryCancelled, rosterFamilyDescendsFrom, isDaemonWorkerProbeTimeout, isSupervisorShutdownAdmissionCancelled, throwIfAdmissionCancelled).\n  2: lines 1100-2200  - worker descriptor / auth helpers: isSessionSummary, isDaemonWorkerDescriptor, workerAuthAdvertisesRoster, workerAuthAdvertisesPeerTransport, descriptorKey, defaultWorkerDescriptorDir, workerSocketPath, normalizeCapabilities.\n  3: lines 2200-3300  - response helpers: withoutCommandId, withoutSupervisorCreateFields, responseWithId, sessionSummariesFromResponse, attachResultFromResponse, cronJobsFromResponse, heartbeatsFromResponse, sortCronJobs, isFinalizedTranscriptEvent, commitWorkerStartupGate, idleEvictionSweepIntervalMs.\n  4: lines 3300-4400  - DaemonSupervisor struct + fields + constructor.\n  5: lines 4400-5500  - DaemonSupervisor roster/worker lifecycle methods.\n  6: lines 5500-6600  - DaemonSupervisor request/session methods.\n  7: lines 6600-7389  - DaemonSupervisor shutdown/eviction + runDaemonSupervisorMode.\nRULES: a `pub fn` in the Rust for EVERY `function` in the TS (29 of them) and a `#[derive]`d pub struct per class, with the same method names. Do not abbreviate or summarise. Do not leave TODO bodies. One `cargo check -p pi-coding-agent` at the very end only.\nIf you run out of budget mid-way, stop cleanly and write in evidence/status/mega-daemon-supervisor.json exactly which passes are done, so the next worker continues."
}