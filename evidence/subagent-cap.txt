Hish's rule (2026-09-11, REVISED after a crash): TOTAL sub-agents must never exceed 15, counting EVERY depth
(depth 1, depth 2, ...). One crashed the session at 20+. Therefore keep at most 13-14 depth-1 children live,
and sub-agents must NEVER spawn their own children (that is what pushes the total over the cap).
Model for every sub-agent: ollama-cloud/deepseek-v4.1-flash only. No fallback model: if it is blocked or out
of tokens, stop and wait for the reset.
